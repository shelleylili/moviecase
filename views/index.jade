extends layout

block content
  //.container
  //    .row
  //        h3.text-center 影片列表如下
  //        each item in movies
  //            .col-md-2.text-center
  //                .thumbnail
  //                    a(href="/movie/#{item._id}")
  //                      img(src="#{item.poster}",alt="#{item.title}")
  //                    .caption
  //                        h3 #{item.title}
  //                        p: a.btn.btn-primary(href="/movie/#{item._id}",role="button") 观看预告片
  include includes/header